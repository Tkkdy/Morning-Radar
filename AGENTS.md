# Morning Radar 项目协作规则

## 范围

- 保持 v0.1 为一个 Python 模块化单体，不扩大产品边界。
- 不引入微服务、数据库服务、向量数据库、重型 Agent 或前端框架。
- 生产 AI 只实现 `OpenAIProvider`；测试使用 `FakeAIProvider`。
- 配置与业务逻辑分离，新增主题、来源、公司和仓库应只改 YAML。

## 数据与安全

- AI 不得创建、猜测或改写 URL；展示链接必须来自采集输入。
- 不保存文章完整正文，只保存元数据、短摘录、分析和来源链接。
- 不提交密钥、Token、UID、个人信息或含真实秘密的 `.env`。
- 所有网络请求必须有超时、有限重试和明确 User-Agent。
- 单一来源失败只能记录并降级，不能导致整条流水线失败。

## 工程要求

- 保持代码简单、可读、有类型标注；避免为未来功能过度抽象。
- 模型输出必须经过 Pydantic 结构校验后才能进入业务流程。
- 新信息源必须同时提供 Fixture 和不访问网络的测试。
- 每次改动后运行相关测试；里程碑完成后运行该阶段全部测试。
- 每个提交只完成一类改动，并说明“做了什么”和“为什么”。

